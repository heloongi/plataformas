using UnityEngine;

public class MoveCommand : MonoBehaviour{}
/*{
   /* private PuzzlePiece pieceA, pieceB;

   // public MoveCommand(PuzzlePiece a, PuzzlePiece b)
    {
        pieceA = a;
        pieceB = b;
    }

    public void Execute()
    {
       // SwapPositions(pieceA, pieceB);
    }

    public void Undo()
    {
       // SwapPositions(pieceA, pieceB); // troca de novo para desfazer
    }

    //private void SwapPositions(Piece a, Piece b)
    {
        Vector3 temp = a.transform.position;
        a.transform.position = b.transform.position;
        b.transform.position = temp;

        int tempIndex = a.CurrentIndex;
        a.CurrentIndex = b.CurrentIndex;
        b.CurrentIndex = tempIndex;
    }
}*/
